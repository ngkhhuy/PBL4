<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="https://cdn-icons-png.flaticon.com/512/1037/1037316.png" type="image/png">
    <title>Welcome Page</title>
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url('https://www.pcworld.com/wp-content/uploads/2024/04/GettyImages-1449917811.jpg?quality=50&strip=all&w=1024') no-repeat center center/cover;
            color: #fff;
            overflow: hidden;
            position: relative;
        }
        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 1;
        }
        .welcome-container {
            text-align: center;
            z-index: 2;
            animation: fadeIn 2s ease-in-out;
        }
        .welcome-title {
            font-size: 4rem;
            margin-bottom: 0.5rem;
            letter-spacing: 2px;
        }
        .welcome-subtitle {
            font-size: 1.75rem;
            margin-bottom: 2rem;
        }
        .start-btn {
            background: #00f2fe;
            border: none;
            padding: 1rem 2.5rem;
            font-size: 1.25rem;
            border-radius: 50px;
            cursor: pointer;
            color: #4facfe;
            font-weight: bold;
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .start-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 10px 20px rgba(0, 242, 254, 0.5);
        }
        .floating-circle {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.2);
            animation: float 6s infinite linear;
        }
        .circle1 {
            width: 150px;
            height: 150px;
            top: 10%;
            left: 15%;
        }
        .circle2 {
            width: 100px;
            height: 100px;
            bottom: 20%;
            right: 10%;
        }
        .circle3 {
            width: 120px;
            height: 120px;
            top: 50%;
            left: 70%;
        }
        @keyframes float {
            0% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-20px);
            }
            100% {
                transform: translateY(0);
            }
        }
        @keyframes fadeIn {
            0% {
                opacity: 0;
            }
            100% {
                opacity: 1;
            }
        }
    </style>
</head>
<body>
    <div class="overlay"></div>
    <div class="welcome-container">
        <h1 class="welcome-title">Welcome to Fast File Transfer</h1>
        <p class="welcome-subtitle">Share large files securely and effortlessly.</p>
        <button class="start-btn" onclick="navigateToMain()">Get Started</button>
    </div>
    <div class="floating-circle circle1"></div>
    <div class="floating-circle circle2"></div>
    <div class="floating-circle circle3"></div>

    <script>
        function navigateToMain() {
            window.location.href = '/register'; // Đường dẫn đến trang chính của bạn
        }
    </script>
</body>
</html>
    